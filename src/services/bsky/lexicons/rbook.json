{
  "lexicon": 1,
  "id": "cc.readup.rfile",
  "description": "A record for uploading and tracking binary blob files with metadata.",
  "defs": {
    "metadata": {
      "type": "object",
      "description": "Book metadata describing the uploaded book.",
      "properties": {
        "title": {
          "type": "string",
          "description": "User-visible book name.",
          "maxLength": 512
        },
        "hash": {
          "type": "string"
        },
        "format": {
          "type": "string",
          "description": "Book format, e.g. 'PDF, epub'.",
          "maxLength": 255
        },
        "author": {
          "type": "string"
        },
        "fileSize": {
          "type": "integer",
          "description": "File size in bytes.",
          "minimum": 0,
          "maximum": 1000000000
        },
        "primaryLanguage": {
          "type": "string"
        },
        "createdAt": {
          "type": "integer"
        },
        "updatedAt": {
          "type": "integer"
        }
      },
      "required": ["title", "hash", "format", "author"]
    },

    "main": {
      "type": "record",
      "description": "A record representing an uploaded book.",
      "key": "any",
      "record": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "The name of the book."
          },
          "coverblob": {
            "type": "blob",
            "accept": ["*/*"],
            "maxSize": 1000000000,
            "description": "The uploaded blob reference of book cover. Note: Individual PDS instances may enforce lower size limits."
          },
          "docblob": {
            "type": "blob",
            "accept": ["*/*"],
            "maxSize": 1000000000,
            "description": "The uploaded blob reference of book doc. Note: Individual PDS instances may enforce lower size limits."
          },
          "config": {
            "type": "blob",
            "accept": ["*/*"],
            "maxSize": 1000000000,
            "description": "The config json file blob reference of book including reading setting, progress, notes, etc. Note: Individual PDS instances may enforce lower size limits."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime",
            "description": "Timestamp when this record was created."
          },
          "bookmeta": {
            "type": "ref",
            "ref": "#metadata",
            "description": "Metadata about the book."
          },
          "checksum": {
            "type": "string",
            "description": "cryptographic checksum for integrity verification."
          },
          "attribution": {
            "type": "string",
            "format": "at-identifier",
            "description": "Handle or DID of the account to attribute this upload to."
          }
        },
        "required": ["name", "createdAt", "bookmeta"]
      }
    }
  }
}
